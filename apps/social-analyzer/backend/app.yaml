command: ['python', '-m', 'uvicorn', 'main:app', '--host', '0.0.0.0', '--port', '8000']
env:
  - name: WAREHOUSE_ID
    value: "50e0bc7f9918a201"
  - name: CATALOG
    value: "danonedemo_catalog"
  - name: SCHEMA
    value: "marketing"
  - name: AI_ENDPOINT_NAME
    value: "databricks-gpt-5-4"
  # DATABRICKS_TOKEN and DATABRICKS_HOST are auto-injected by Databricks Apps runtime.
  # Set them manually only for local development via a .env file (never commit secrets).
